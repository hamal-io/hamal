package io.hamal.agent.extension.std

import io.hamal.lib.script.api.Context

object ConsoleLog : Log {
    override fun invoke(level: LogLevel, ctx: Context) {
        // get format from config
        // format values
        // print
    }
}