import React from "react";

interface FooterProps {
    toggleSettings: () => void;
    showSettings: boolean;
}

export default (props: FooterProps) => {
    return (
        <div>

        </div>
    );
};
