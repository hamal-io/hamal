export type NodeId = string


export type CanvasState = {
    scale: number;
    position: Position;
}

export type Position = {
    x: number;
    y: number;
};

export type Size = {
    width: number;
    height: number;
};