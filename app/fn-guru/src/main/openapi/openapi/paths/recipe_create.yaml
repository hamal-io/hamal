post:
  summary: Create a recipe
  description: Create a recipe
  tags:
    - Recipe
  operationId: recipe/create
  parameters:
    - $ref: ../components/parameters/group_id.yaml
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/requestBodies/recipe_create_req.yaml
    required: true
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: ../components/responses/recipe_create_res.yaml
    '400':
      description: Bad Request
  x-codeSamples:
    - lang: 'cUrl'
      label: curl
      source: |
        curl --request POST \
         --url 'https://hamal.io/v1/namespaces/1/recipes' \
         --header 'content-type: application/json: ' \
         --data '{
          "name" : "MyNewRecipe",
          "value" : "print('Code for the recipe')"
         }'
    - lang: 'Lua'
      label: hamal
      source:
        $ref: ../code_samples/recipe_create.lua

